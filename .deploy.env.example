# Generated by scripts/deploy.sh; consumed by scripts/compose-prod.sh.
# First deploy: export ECR_REGISTRY from the Terraform ecr_registry output.
# The wrapper resolves the commit and writes IMAGE_TAG automatically.
ECR_REGISTRY=
IMAGE_TAG=
